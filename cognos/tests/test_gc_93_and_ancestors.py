"""Anthony Labarre © 2024

This file was automatically generated by generate_tests.py on 2024-07-13. It
consists of a test suite for recognizers that identify gc_93 and / or all its
ancestors. Depending on which recognizers have been implemented, some class ids
may have been substituted with equivalent ids according to ISGCI.

Some of the ancestors may have been purposefully omitted from this file, either
in order to avoid duplicating tests or because the needed recognizers were
missing.

Check https://www.graphclasses.org/classes/gc_93 for more information.

"""
# Imports ---------------------------------------------------------------------
import graph_recognition.fisc_based_recognizers
import graph_recognition.profitable_hereditary_n
import graph_recognition.profitable_hereditary_n_4
import graph_recognition.recognizers_n
import graph_recognition.recognizers_n_2
import graph_recognition.recognizers_n_3
import graph_recognition.recognizers_n_4
import graph_recognition.recognizers_n_5
import networkx
import networkx.algorithms.chordal
import os
import sys
import unittest


class Test_gc_93_and_ancestors(unittest.TestCase):
    """A generic test case for class gc_93 and all its ancestors that have not
    already been covered by other tests."""
    @classmethod
    def setUpClass(self):
        """Stores positive and negative instances to test."""
        super(Test_gc_93_and_ancestors, self).setUpClass()
        self.positive = list()
        basedir = "tests/test_data/block=gc_93"
        print(self.__class__.__qualname__.join("[]"), "initialising positive instances from tests/test_data/block=gc_93...", end=" ")
        sys.stdout.flush()
        for dataset in sorted(os.listdir(basedir), key=lambda x: os.stat(os.path.join(basedir, x)).st_size)[:5]:
            if dataset.endswith(".g6"):
                data = networkx.read_graph6(os.path.join(basedir, dataset))
            elif dataset.endswith(".s6"):
                data = networkx.read_sparse6(os.path.join(basedir, dataset))
            else:
                continue
            if not isinstance(data, list):
                data = [data]
            self.positive.extend(data)
        print("done.")

    def test_gc_93(self):
        """Tests positive instances for class gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.profitable_hereditary_n.is_block(g) for g in self.positive))
        print("done.")
    def test_AUTO_92(self):
        """Tests positive instances for class AUTO_92. AUTO_92 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_92(g) for g in self.positive))
        print("done.")
    def test_AUTO_1820(self):
        """Tests positive instances for class AUTO_1820. AUTO_1820 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1820(g) for g in self.positive))
        print("done.")
    def test_gc_441(self):
        """Tests positive instances for class gc_441. gc_441 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_441(g) for g in self.positive))
        print("done.")
    def test_gc_774(self):
        """Tests positive instances for class gc_774. gc_774 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_gc_774(g) for g in self.positive))
        print("done.")
    def test_gc_1240(self):
        """Tests positive instances for class gc_1240. gc_1240 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_gc_1240(g) for g in self.positive))
        print("done.")
    def test_gc_177(self):
        """Tests positive instances for class gc_177. gc_177 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhdg_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_202(self):
        """Tests positive instances for class AUTO_202. AUTO_202 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_202(g) for g in self.positive))
        print("done.")
    def test_AUTO_1473(self):
        """Tests positive instances for class AUTO_1473. AUTO_1473 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1473(g) for g in self.positive))
        print("done.")
    def test_gc_490(self):
        """Tests positive instances for class gc_490. gc_490 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_359(g) for g in self.positive))
        print("done.")
    def test_gc_1032(self):
        """Tests positive instances for class gc_1032. gc_1032 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_co_fork_hole_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_94(self):
        """Tests positive instances for class AUTO_94. AUTO_94 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_anti_hole_free(g) for g in self.positive))
        print("done.")
    def test_gc_628(self):
        """Tests positive instances for class gc_628. gc_628 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_628(g) for g in self.positive))
        print("done.")
    def test_gc_248(self):
        """Tests positive instances for class gc_248. gc_248 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_chordal_or_co_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_113(self):
        """Tests positive instances for class gc_113. gc_113 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhp_free(g) for g in self.positive))
        print("done.")
    def test_gc_618(self):
        """Tests positive instances for class gc_618. gc_618 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_618(g) for g in self.positive))
        print("done.")
    def test_gc_376(self):
        """Tests positive instances for class gc_376. gc_376 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_376(g) for g in self.positive))
        print("done.")
    def test_gc_195(self):
        """Tests positive instances for class gc_195. gc_195 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_2.is_bigeodetic(g) for g in self.positive))
        print("done.")
    def test_AUTO_738(self):
        """Tests positive instances for class AUTO_738. AUTO_738 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_chordal_and_gem_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_1470(self):
        """Tests positive instances for class AUTO_1470. AUTO_1470 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1470(g) for g in self.positive))
        print("done.")
    def test_AUTO_3(self):
        """Tests positive instances for class AUTO_3. AUTO_3 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_3(g) for g in self.positive))
        print("done.")
    def test_gc_96(self):
        """Tests positive instances for class gc_96. gc_96 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_2.is_geodetic(g) for g in self.positive))
        print("done.")
    def test_gc_473(self):
        """Tests positive instances for class gc_473. gc_473 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.profitable_hereditary_n_4.is_gc_473(g) for g in self.positive))
        print("done.")
    def test_gc_700(self):
        """Tests positive instances for class gc_700. gc_700 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_700(g) for g in self.positive))
        print("done.")
    def test_gc_1254(self):
        """Tests positive instances for class gc_1254. gc_1254 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_2.is_locally_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_772(self):
        """Tests positive instances for class gc_772. gc_772 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_gc_772(g) for g in self.positive))
        print("done.")
    def test_gc_619(self):
        """Tests positive instances for class gc_619. gc_619 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_619(g) for g in self.positive))
        print("done.")
    def test_AUTO_41(self):
        """Tests positive instances for class AUTO_41. AUTO_41 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_41(g) for g in self.positive))
        print("done.")
    def test_gc_1350(self):
        """Tests positive instances for class gc_1350. gc_1350 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_cnplus4_dart_gem_free(g) for g in self.positive))
        print("done.")
    def test_gc_614(self):
        """Tests positive instances for class gc_614. gc_614 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_3.is_pseudo_median(g) for g in self.positive))
        print("done.")
    def test_AUTO_224(self):
        """Tests positive instances for class AUTO_224. AUTO_224 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_224(g) for g in self.positive))
        print("done.")
    def test_gc_592(self):
        """Tests positive instances for class gc_592. gc_592 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_592(g) for g in self.positive))
        print("done.")
    def test_gc_435(self):
        """Tests positive instances for class gc_435. gc_435 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_weakly_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_609(self):
        """Tests positive instances for class gc_609. gc_609 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_pretty(g) for g in self.positive))
        print("done.")
    def test_AUTO_497(self):
        """Tests positive instances for class AUTO_497. AUTO_497 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_497(g) for g in self.positive))
        print("done.")
    def test_gc_960(self):
        """Tests positive instances for class gc_960. gc_960 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_960(g) for g in self.positive))
        print("done.")
    def test_gc_30(self):
        """Tests positive instances for class gc_30. gc_30 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.isCnplus4S3Free(g) for g in self.positive))
        print("done.")
    def test_gc_54(self):
        """Tests positive instances for class gc_54. gc_54 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_5.is_clique_helly(g) for g in self.positive))
        print("done.")
    def test_gc_1281(self):
        """Tests positive instances for class gc_1281. gc_1281 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1281(g) for g in self.positive))
        print("done.")
    def test_gc_354(self):
        """Tests positive instances for class gc_354. gc_354 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_354(g) for g in self.positive))
        print("done.")
    def test_gc_613(self):
        """Tests positive instances for class gc_613. gc_613 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_xc_10_free(g) for g in self.positive))
        print("done.")
    def test_gc_201(self):
        """Tests positive instances for class gc_201. gc_201 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_3.is_pseudo_modular(g) for g in self.positive))
        print("done.")
    def test_AUTO_1443(self):
        """Tests positive instances for class AUTO_1443. AUTO_1443 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1443(g) for g in self.positive))
        print("done.")
    def test_gc_437(self):
        """Tests positive instances for class gc_437. gc_437 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hole_free(g) for g in self.positive))
        print("done.")
    def test_gc_585(self):
        """Tests positive instances for class gc_585. gc_585 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_585(g) for g in self.positive))
        print("done.")
    def test_gc_360(self):
        """Tests positive instances for class gc_360. gc_360 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_360(g) for g in self.positive))
        print("done.")
    def test_AUTO_2097(self):
        """Tests positive instances for class AUTO_2097. AUTO_2097 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2097(g) for g in self.positive))
        print("done.")
    def test_gc_436(self):
        """Tests positive instances for class gc_436. gc_436 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_436(g) for g in self.positive))
        print("done.")
    def test_gc_814(self):
        """Tests positive instances for class gc_814. gc_814 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_814(g) for g in self.positive))
        print("done.")
    def test_gc_972(self):
        """Tests positive instances for class gc_972. gc_972 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_972(g) for g in self.positive))
        print("done.")
    def test_gc_400(self):
        """Tests positive instances for class gc_400. gc_400 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_5.is_k_23_p_hole_free(g) for g in self.positive))
        print("done.")
    def test_gc_1352(self):
        """Tests positive instances for class gc_1352. gc_1352 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_cnplus4_co_k3_union3_k1_dart_gem_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_2123(self):
        """Tests positive instances for class AUTO_2123. AUTO_2123 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2123(g) for g in self.positive))
        print("done.")
    def test_AUTO_1490(self):
        """Tests positive instances for class AUTO_1490. AUTO_1490 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1490(g) for g in self.positive))
        print("done.")
    def test_gc_382(self):
        """Tests positive instances for class gc_382. gc_382 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(networkx.algorithms.chordal.is_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_1026(self):
        """Tests positive instances for class gc_1026. gc_1026 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1026(g) for g in self.positive))
        print("done.")
    def test_gc_590(self):
        """Tests positive instances for class gc_590. gc_590 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_590(g) for g in self.positive))
        print("done.")
    def test_AUTO_2096(self):
        """Tests positive instances for class AUTO_2096. AUTO_2096 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2096(g) for g in self.positive))
        print("done.")
    def test_AUTO_329(self):
        """Tests positive instances for class AUTO_329. AUTO_329 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_329(g) for g in self.positive))
        print("done.")
    def test_gc_216(self):
        """Tests positive instances for class gc_216. gc_216 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhg_free(g) for g in self.positive))
        print("done.")
    def test_gc_351(self):
        """Tests positive instances for class gc_351. gc_351 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhda_free(g) for g in self.positive))
        print("done.")
    def test_gc_228(self):
        """Tests positive instances for class gc_228. gc_228 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hh_free(g) for g in self.positive))
        print("done.")
    def test_gc_367(self):
        """Tests positive instances for class gc_367. gc_367 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_367(g) for g in self.positive))
        print("done.")
    def test_gc_779(self):
        """Tests positive instances for class gc_779. gc_779 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_779(g) for g in self.positive))
        print("done.")
    def test_AUTO_2110(self):
        """Tests positive instances for class AUTO_2110. AUTO_2110 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2110(g) for g in self.positive))
        print("done.")
    def test_AUTO_1509(self):
        """Tests positive instances for class AUTO_1509. AUTO_1509 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1509(g) for g in self.positive))
        print("done.")
    def test_gc_230(self):
        """Tests positive instances for class gc_230. gc_230 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhd_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_1491(self):
        """Tests positive instances for class AUTO_1491. AUTO_1491 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1491(g) for g in self.positive))
        print("done.")
    def test_AUTO_1510(self):
        """Tests positive instances for class AUTO_1510. AUTO_1510 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1510(g) for g in self.positive))
        print("done.")
    def test_gc_433(self):
        """Tests positive instances for class gc_433. gc_433 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_433(g) for g in self.positive))
        print("done.")
    def test_AUTO_1515(self):
        """Tests positive instances for class AUTO_1515. AUTO_1515 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1515(g) for g in self.positive))
        print("done.")
    def test_AUTO_2104(self):
        """Tests positive instances for class AUTO_2104. AUTO_2104 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2104(g) for g in self.positive))
        print("done.")
    def test_gc_588(self):
        """Tests positive instances for class gc_588. gc_588 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_588(g) for g in self.positive))
        print("done.")
    def test_gc_362(self):
        """Tests positive instances for class gc_362. gc_362 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_362(g) for g in self.positive))
        print("done.")
    def test_gc_361(self):
        """Tests positive instances for class gc_361. gc_361 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_house_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_407(self):
        """Tests positive instances for class AUTO_407. AUTO_407 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_407(g) for g in self.positive))
        print("done.")
    def test_gc_748(self):
        """Tests positive instances for class gc_748. gc_748 is an ancestor of gc_93."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_748(g) for g in self.positive))
        print("done.")