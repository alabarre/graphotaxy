"""Anthony Labarre © 2024

This file was automatically generated by generate_tests.py on 2024-07-13. It
consists of a test suite for recognizers that identify gc_39 and / or all its
ancestors. Depending on which recognizers have been implemented, some class ids
may have been substituted with equivalent ids according to ISGCI.

Some of the ancestors may have been purposefully omitted from this file, either
in order to avoid duplicating tests or because the needed recognizers were
missing.

Check https://www.graphclasses.org/classes/gc_39 for more information.

"""
# Imports ---------------------------------------------------------------------
import graph_recognition.fisc_based_recognizers
import graph_recognition.profitable_hereditary_n
import graph_recognition.profitable_hereditary_n_4
import graph_recognition.recognizers_n
import graph_recognition.recognizers_n_2
import graph_recognition.recognizers_n_4
import graph_recognition.recognizers_n_5
import networkx
import networkx.algorithms.chordal
import os
import sys
import unittest


class Test_gc_39_and_ancestors(unittest.TestCase):
    """A generic test case for class gc_39 and all its ancestors that have not
    already been covered by other tests."""
    @classmethod
    def setUpClass(self):
        """Stores positive and negative instances to test."""
        super(Test_gc_39_and_ancestors, self).setUpClass()
        self.positive = list()
        basedir = "tests/test_data/split=gc_39"
        print(self.__class__.__qualname__.join("[]"), "initialising positive instances from tests/test_data/split=gc_39...", end=" ")
        sys.stdout.flush()
        for dataset in sorted(os.listdir(basedir), key=lambda x: os.stat(os.path.join(basedir, x)).st_size)[:5]:
            if dataset.endswith(".g6"):
                data = networkx.read_graph6(os.path.join(basedir, dataset))
            elif dataset.endswith(".s6"):
                data = networkx.read_sparse6(os.path.join(basedir, dataset))
            else:
                continue
            if not isinstance(data, list):
                data = [data]
            self.positive.extend(data)
        print("done.")

    def test_gc_39(self):
        """Tests positive instances for class gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.profitable_hereditary_n.is_gc_313(g) for g in self.positive))
        print("done.")
    def test_AUTO_92(self):
        """Tests positive instances for class AUTO_92. AUTO_92 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_92(g) for g in self.positive))
        print("done.")
    def test_AUTO_3827(self):
        """Tests positive instances for class AUTO_3827. AUTO_3827 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_b_perfect_and_chordal(g) for g in self.positive))
        print("done.")
    def test_AUTO_1820(self):
        """Tests positive instances for class AUTO_1820. AUTO_1820 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1820(g) for g in self.positive))
        print("done.")
    def test_gc_404(self):
        """Tests positive instances for class gc_404. gc_404 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_404(g) for g in self.positive))
        print("done.")
    def test_AUTO_1474(self):
        """Tests positive instances for class AUTO_1474. AUTO_1474 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1474(g) for g in self.positive))
        print("done.")
    def test_gc_1276(self):
        """Tests positive instances for class gc_1276. gc_1276 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1276(g) for g in self.positive))
        print("done.")
    def test_AUTO_1531(self):
        """Tests positive instances for class AUTO_1531. AUTO_1531 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_p5_co_a_co_domino_antihole_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_2142(self):
        """Tests positive instances for class AUTO_2142. AUTO_2142 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2142(g) for g in self.positive))
        print("done.")
    def test_AUTO_1461(self):
        """Tests positive instances for class AUTO_1461. AUTO_1461 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1461(g) for g in self.positive))
        print("done.")
    def test_AUTO_202(self):
        """Tests positive instances for class AUTO_202. AUTO_202 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_202(g) for g in self.positive))
        print("done.")
    def test_AUTO_1538(self):
        """Tests positive instances for class AUTO_1538. AUTO_1538 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_p5_co_p_anti_hole_free(g) for g in self.positive))
        print("done.")
    def test_gc_819(self):
        """Tests positive instances for class gc_819. gc_819 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_819(g) for g in self.positive))
        print("done.")
    def test_AUTO_1511(self):
        """Tests positive instances for class AUTO_1511. AUTO_1511 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1511(g) for g in self.positive))
        print("done.")
    def test_AUTO_1473(self):
        """Tests positive instances for class AUTO_1473. AUTO_1473 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1473(g) for g in self.positive))
        print("done.")
    def test_AUTO_1514(self):
        """Tests positive instances for class AUTO_1514. AUTO_1514 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1514(g) for g in self.positive))
        print("done.")
    def test_gc_403(self):
        """Tests positive instances for class gc_403. gc_403 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_403(g) for g in self.positive))
        print("done.")
    def test_gc_224(self):
        """Tests positive instances for class gc_224. gc_224 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_224(g) for g in self.positive))
        print("done.")
    def test_gc_490(self):
        """Tests positive instances for class gc_490. gc_490 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_359(g) for g in self.positive))
        print("done.")
    def test_gc_537(self):
        """Tests positive instances for class gc_537. gc_537 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_537(g) for g in self.positive))
        print("done.")
    def test_gc_1035(self):
        """Tests positive instances for class gc_1035. gc_1035 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1035(g) for g in self.positive))
        print("done.")
    def test_AUTO_1822(self):
        """Tests positive instances for class AUTO_1822. AUTO_1822 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1822(g) for g in self.positive))
        print("done.")
    def test_gc_260(self):
        """Tests positive instances for class gc_260. gc_260 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_260(g) for g in self.positive))
        print("done.")
    def test_gc_840(self):
        """Tests positive instances for class gc_840. gc_840 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_840(g) for g in self.positive))
        print("done.")
    def test_AUTO_94(self):
        """Tests positive instances for class AUTO_94. AUTO_94 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_anti_hole_free(g) for g in self.positive))
        print("done.")
    def test_gc_678(self):
        """Tests positive instances for class gc_678. gc_678 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_678(g) for g in self.positive))
        print("done.")
    def test_AUTO_2124(self):
        """Tests positive instances for class AUTO_2124. AUTO_2124 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2124(g) for g in self.positive))
        print("done.")
    def test_gc_113(self):
        """Tests positive instances for class gc_113. gc_113 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhp_free(g) for g in self.positive))
        print("done.")
    def test_gc_248(self):
        """Tests positive instances for class gc_248. gc_248 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_chordal_or_co_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_625(self):
        """Tests positive instances for class gc_625. gc_625 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_625(g) for g in self.positive))
        print("done.")
    def test_gc_811(self):
        """Tests positive instances for class gc_811. gc_811 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_811(g) for g in self.positive))
        print("done.")
    def test_AUTO_3856(self):
        """Tests positive instances for class AUTO_3856. AUTO_3856 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_co_b_perfect_and_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_666(self):
        """Tests positive instances for class gc_666. gc_666 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is2K32P3Cnplus4K3UnionP3Free(g) for g in self.positive))
        print("done.")
    def test_gc_430(self):
        """Tests positive instances for class gc_430. gc_430 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_430(g) for g in self.positive))
        print("done.")
    def test_AUTO_1456(self):
        """Tests positive instances for class AUTO_1456. AUTO_1456 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1456(g) for g in self.positive))
        print("done.")
    def test_AUTO_1470(self):
        """Tests positive instances for class AUTO_1470. AUTO_1470 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1470(g) for g in self.positive))
        print("done.")
    def test_gc_808(self):
        """Tests positive instances for class gc_808. gc_808 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_808(g) for g in self.positive))
        print("done.")
    def test_gc_1254(self):
        """Tests positive instances for class gc_1254. gc_1254 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_2.is_locally_chordal(g) for g in self.positive))
        print("done.")
    def test_AUTO_2141(self):
        """Tests positive instances for class AUTO_2141. AUTO_2141 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2141(g) for g in self.positive))
        print("done.")
    def test_gc_511(self):
        """Tests positive instances for class gc_511. gc_511 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_511(g) for g in self.positive))
        print("done.")
    def test_AUTO_1512(self):
        """Tests positive instances for class AUTO_1512. AUTO_1512 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1512(g) for g in self.positive))
        print("done.")
    def test_gc_428(self):
        """Tests positive instances for class gc_428. gc_428 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_428(g) for g in self.positive))
        print("done.")
    def test_AUTO_41(self):
        """Tests positive instances for class AUTO_41. AUTO_41 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_41(g) for g in self.positive))
        print("done.")
    def test_gc_1361(self):
        """Tests positive instances for class gc_1361. gc_1361 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_bipartite_or_co_bipartite_or_split(g) for g in self.positive))
        print("done.")
    def test_AUTO_1460(self):
        """Tests positive instances for class AUTO_1460. AUTO_1460 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1460(g) for g in self.positive))
        print("done.")
    def test_gc_679(self):
        """Tests positive instances for class gc_679. gc_679 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_p5_anti_hole_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_2102(self):
        """Tests positive instances for class AUTO_2102. AUTO_2102 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2102(g) for g in self.positive))
        print("done.")
    def test_AUTO_224(self):
        """Tests positive instances for class AUTO_224. AUTO_224 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_224(g) for g in self.positive))
        print("done.")
    def test_gc_816(self):
        """Tests positive instances for class gc_816. gc_816 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_816(g) for g in self.positive))
        print("done.")
    def test_gc_930(self):
        """Tests positive instances for class gc_930. gc_930 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_930(g) for g in self.positive))
        print("done.")
    def test_gc_1279(self):
        """Tests positive instances for class gc_1279. gc_1279 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1279(g) for g in self.positive))
        print("done.")
    def test_gc_813(self):
        """Tests positive instances for class gc_813. gc_813 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_813(g) for g in self.positive))
        print("done.")
    def test_gc_647(self):
        """Tests positive instances for class gc_647. gc_647 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_647(g) for g in self.positive))
        print("done.")
    def test_gc_592(self):
        """Tests positive instances for class gc_592. gc_592 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_592(g) for g in self.positive))
        print("done.")
    def test_gc_435(self):
        """Tests positive instances for class gc_435. gc_435 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_weakly_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_1217(self):
        """Tests positive instances for class gc_1217. gc_1217 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1217(g) for g in self.positive))
        print("done.")
    def test_gc_456(self):
        """Tests positive instances for class gc_456. gc_456 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_456(g) for g in self.positive))
        print("done.")
    def test_gc_609(self):
        """Tests positive instances for class gc_609. gc_609 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_pretty(g) for g in self.positive))
        print("done.")
    def test_gc_324(self):
        """Tests positive instances for class gc_324. gc_324 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_324(g) for g in self.positive))
        print("done.")
    def test_AUTO_497(self):
        """Tests positive instances for class AUTO_497. AUTO_497 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_497(g) for g in self.positive))
        print("done.")
    def test_gc_648(self):
        """Tests positive instances for class gc_648. gc_648 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_648(g) for g in self.positive))
        print("done.")
    def test_AUTO_2773(self):
        """Tests positive instances for class AUTO_2773. AUTO_2773 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.isK33K33PlusE2P3Cnplus4Free(g) for g in self.positive))
        print("done.")
    def test_AUTO_1462(self):
        """Tests positive instances for class AUTO_1462. AUTO_1462 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1462(g) for g in self.positive))
        print("done.")
    def test_AUTO_2108(self):
        """Tests positive instances for class AUTO_2108. AUTO_2108 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.isK333CoCnplus4Free(g) for g in self.positive))
        print("done.")
    def test_gc_1275(self):
        """Tests positive instances for class gc_1275. gc_1275 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1275(g) for g in self.positive))
        print("done.")
    def test_gc_1281(self):
        """Tests positive instances for class gc_1281. gc_1281 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1281(g) for g in self.positive))
        print("done.")
    def test_gc_432(self):
        """Tests positive instances for class gc_432. gc_432 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_432(g) for g in self.positive))
        print("done.")
    def test_gc_534(self):
        """Tests positive instances for class gc_534. gc_534 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hereditary_welsh_powell_perfect(g) for g in self.positive))
        print("done.")
    def test_gc_429(self):
        """Tests positive instances for class gc_429. gc_429 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_429(g) for g in self.positive))
        print("done.")
    def test_gc_273(self):
        """Tests positive instances for class gc_273. gc_273 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_273(g) for g in self.positive))
        print("done.")
    def test_gc_735(self):
        """Tests positive instances for class gc_735. gc_735 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_735(g) for g in self.positive))
        print("done.")
    def test_gc_659(self):
        """Tests positive instances for class gc_659. gc_659 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is3K_3Cnplus4Free(g) for g in self.positive))
        print("done.")
    def test_gc_638(self):
        """Tests positive instances for class gc_638. gc_638 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_638(g) for g in self.positive))
        print("done.")
    def test_gc_437(self):
        """Tests positive instances for class gc_437. gc_437 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hole_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_1513(self):
        """Tests positive instances for class AUTO_1513. AUTO_1513 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1513(g) for g in self.positive))
        print("done.")
    def test_AUTO_1465(self):
        """Tests positive instances for class AUTO_1465. AUTO_1465 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1465(g) for g in self.positive))
        print("done.")
    def test_AUTO_1821(self):
        """Tests positive instances for class AUTO_1821. AUTO_1821 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1821(g) for g in self.positive))
        print("done.")
    def test_gc_683(self):
        """Tests positive instances for class gc_683. gc_683 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_683(g) for g in self.positive))
        print("done.")
    def test_AUTO_2097(self):
        """Tests positive instances for class AUTO_2097. AUTO_2097 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2097(g) for g in self.positive))
        print("done.")
    def test_gc_360(self):
        """Tests positive instances for class gc_360. gc_360 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_360(g) for g in self.positive))
        print("done.")
    def test_AUTO_1464(self):
        """Tests positive instances for class AUTO_1464. AUTO_1464 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1464(g) for g in self.positive))
        print("done.")
    def test_gc_436(self):
        """Tests positive instances for class gc_436. gc_436 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_436(g) for g in self.positive))
        print("done.")
    def test_gc_756(self):
        """Tests positive instances for class gc_756. gc_756 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_756(g) for g in self.positive))
        print("done.")
    def test_gc_814(self):
        """Tests positive instances for class gc_814. gc_814 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_814(g) for g in self.positive))
        print("done.")
    def test_gc_410(self):
        """Tests positive instances for class gc_410. gc_410 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_410(g) for g in self.positive))
        print("done.")
    def test_AUTO_1459(self):
        """Tests positive instances for class AUTO_1459. AUTO_1459 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1459(g) for g in self.positive))
        print("done.")
    def test_gc_1002(self):
        """Tests positive instances for class gc_1002. gc_1002 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1002(g) for g in self.positive))
        print("done.")
    def test_gc_669(self):
        """Tests positive instances for class gc_669. gc_669 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_669(g) for g in self.positive))
        print("done.")
    def test_gc_400(self):
        """Tests positive instances for class gc_400. gc_400 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_5.is_k_23_p_hole_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_734(self):
        """Tests positive instances for class AUTO_734. AUTO_734 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_734(g) for g in self.positive))
        print("done.")
    def test_gc_431(self):
        """Tests positive instances for class gc_431. gc_431 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_431(g) for g in self.positive))
        print("done.")
    def test_AUTO_2123(self):
        """Tests positive instances for class AUTO_2123. AUTO_2123 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2123(g) for g in self.positive))
        print("done.")
    def test_AUTO_1490(self):
        """Tests positive instances for class AUTO_1490. AUTO_1490 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1490(g) for g in self.positive))
        print("done.")
    def test_gc_382(self):
        """Tests positive instances for class gc_382. gc_382 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(networkx.algorithms.chordal.is_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_845(self):
        """Tests positive instances for class gc_845. gc_845 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_845(g) for g in self.positive))
        print("done.")
    def test_gc_268(self):
        """Tests positive instances for class gc_268. gc_268 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_268(g) for g in self.positive))
        print("done.")
    def test_AUTO_1525(self):
        """Tests positive instances for class AUTO_1525. AUTO_1525 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1525(g) for g in self.positive))
        print("done.")
    def test_gc_590(self):
        """Tests positive instances for class gc_590. gc_590 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_590(g) for g in self.positive))
        print("done.")
    def test_gc_757(self):
        """Tests positive instances for class gc_757. gc_757 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_757(g) for g in self.positive))
        print("done.")
    def test_AUTO_2096(self):
        """Tests positive instances for class AUTO_2096. AUTO_2096 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2096(g) for g in self.positive))
        print("done.")
    def test_gc_1(self):
        """Tests positive instances for class gc_1. gc_1 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1(g) for g in self.positive))
        print("done.")
    def test_gc_1037(self):
        """Tests positive instances for class gc_1037. gc_1037 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1037(g) for g in self.positive))
        print("done.")
    def test_AUTO_329(self):
        """Tests positive instances for class AUTO_329. AUTO_329 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_329(g) for g in self.positive))
        print("done.")
    def test_AUTO_2093(self):
        """Tests positive instances for class AUTO_2093. AUTO_2093 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_co_cnplus4_co_x_59_co_longhorn_free(g) for g in self.positive))
        print("done.")
    def test_gc_812(self):
        """Tests positive instances for class gc_812. gc_812 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_812(g) for g in self.positive))
        print("done.")
    def test_gc_562(self):
        """Tests positive instances for class gc_562. gc_562 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_cnplus4_x_59_longhorn_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_2465(self):
        """Tests positive instances for class AUTO_2465. AUTO_2465 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_2.is_co_locally_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_931(self):
        """Tests positive instances for class gc_931. gc_931 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_931(g) for g in self.positive))
        print("done.")
    def test_gc_396(self):
        """Tests positive instances for class gc_396. gc_396 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_396(g) for g in self.positive))
        print("done.")
    def test_gc_758(self):
        """Tests positive instances for class gc_758. gc_758 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_758(g) for g in self.positive))
        print("done.")
    def test_gc_815(self):
        """Tests positive instances for class gc_815. gc_815 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_815(g) for g in self.positive))
        print("done.")
    def test_gc_759(self):
        """Tests positive instances for class gc_759. gc_759 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_759(g) for g in self.positive))
        print("done.")
    def test_gc_351(self):
        """Tests positive instances for class gc_351. gc_351 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhda_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_2(self):
        """Tests positive instances for class AUTO_2. AUTO_2 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2(g) for g in self.positive))
        print("done.")
    def test_AUTO_1533(self):
        """Tests positive instances for class AUTO_1533. AUTO_1533 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1533(g) for g in self.positive))
        print("done.")
    def test_gc_737(self):
        """Tests positive instances for class gc_737. gc_737 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_737(g) for g in self.positive))
        print("done.")
    def test_gc_394(self):
        """Tests positive instances for class gc_394. gc_394 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.profitable_hereditary_n_4.is_gc_394(g) for g in self.positive))
        print("done.")
    def test_AUTO_1075(self):
        """Tests positive instances for class AUTO_1075. AUTO_1075 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_P5_anti_hole_co_domino_free(g) for g in self.positive))
        print("done.")
    def test_gc_228(self):
        """Tests positive instances for class gc_228. gc_228 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hh_free(g) for g in self.positive))
        print("done.")
    def test_gc_402(self):
        """Tests positive instances for class gc_402. gc_402 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_402(g) for g in self.positive))
        print("done.")
    def test_gc_367(self):
        """Tests positive instances for class gc_367. gc_367 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_367(g) for g in self.positive))
        print("done.")
    def test_AUTO_1476(self):
        """Tests positive instances for class AUTO_1476. AUTO_1476 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1476(g) for g in self.positive))
        print("done.")
    def test_AUTO_2110(self):
        """Tests positive instances for class AUTO_2110. AUTO_2110 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2110(g) for g in self.positive))
        print("done.")
    def test_AUTO_2248(self):
        """Tests positive instances for class AUTO_2248. AUTO_2248 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2248(g) for g in self.positive))
        print("done.")
    def test_gc_665(self):
        """Tests positive instances for class gc_665. gc_665 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_664(g) for g in self.positive))
        print("done.")
    def test_gc_230(self):
        """Tests positive instances for class gc_230. gc_230 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhd_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_2089(self):
        """Tests positive instances for class AUTO_2089. AUTO_2089 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2089(g) for g in self.positive))
        print("done.")
    def test_gc_433(self):
        """Tests positive instances for class gc_433. gc_433 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_433(g) for g in self.positive))
        print("done.")
    def test_gc_563(self):
        """Tests positive instances for class gc_563. gc_563 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_563(g) for g in self.positive))
        print("done.")
    def test_AUTO_1526(self):
        """Tests positive instances for class AUTO_1526. AUTO_1526 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1526(g) for g in self.positive))
        print("done.")
    def test_AUTO_2104(self):
        """Tests positive instances for class AUTO_2104. AUTO_2104 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2104(g) for g in self.positive))
        print("done.")
    def test_AUTO_1524(self):
        """Tests positive instances for class AUTO_1524. AUTO_1524 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1524(g) for g in self.positive))
        print("done.")
    def test_gc_1108(self):
        """Tests positive instances for class gc_1108. gc_1108 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1108(g) for g in self.positive))
        print("done.")
    def test_gc_362(self):
        """Tests positive instances for class gc_362. gc_362 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_362(g) for g in self.positive))
        print("done.")
    def test_AUTO_2077(self):
        """Tests positive instances for class AUTO_2077. AUTO_2077 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_co_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_588(self):
        """Tests positive instances for class gc_588. gc_588 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_588(g) for g in self.positive))
        print("done.")
    def test_AUTO_2088(self):
        """Tests positive instances for class AUTO_2088. AUTO_2088 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_co_hereditary_welsh_powell_perfect(g) for g in self.positive))
        print("done.")
    def test_AUTO_1735(self):
        """Tests positive instances for class AUTO_1735. AUTO_1735 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1735(g) for g in self.positive))
        print("done.")
    def test_gc_818(self):
        """Tests positive instances for class gc_818. gc_818 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_818(g) for g in self.positive))
        print("done.")
    def test_AUTO_1463(self):
        """Tests positive instances for class AUTO_1463. AUTO_1463 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1463(g) for g in self.positive))
        print("done.")
    def test_gc_361(self):
        """Tests positive instances for class gc_361. gc_361 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_house_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_407(self):
        """Tests positive instances for class AUTO_407. AUTO_407 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_407(g) for g in self.positive))
        print("done.")
    def test_gc_401(self):
        """Tests positive instances for class gc_401. gc_401 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_401(g) for g in self.positive))
        print("done.")
    def test_AUTO_2079(self):
        """Tests positive instances for class AUTO_2079. AUTO_2079 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2079(g) for g in self.positive))
        print("done.")
    def test_AUTO_1520(self):
        """Tests positive instances for class AUTO_1520. AUTO_1520 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1520(g) for g in self.positive))
        print("done.")
    def test_AUTO_2125(self):
        """Tests positive instances for class AUTO_2125. AUTO_2125 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2125(g) for g in self.positive))
        print("done.")
    def test_AUTO_1527(self):
        """Tests positive instances for class AUTO_1527. AUTO_1527 is an ancestor of gc_39."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_5.is_k2_u_k3_co_p_antihole_free(g) for g in self.positive))
        print("done.")