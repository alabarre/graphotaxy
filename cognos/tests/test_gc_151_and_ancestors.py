"""Anthony Labarre © 2024

This file was automatically generated by generate_tests.py on 2024-07-13. It
consists of a test suite for recognizers that identify gc_151 and / or all its
ancestors. Depending on which recognizers have been implemented, some class ids
may have been substituted with equivalent ids according to ISGCI.

Some of the ancestors may have been purposefully omitted from this file, either
in order to avoid duplicating tests or because the needed recognizers were
missing.

Check https://www.graphclasses.org/classes/gc_151 for more information.

"""
# Imports ---------------------------------------------------------------------
import graph_recognition.fisc_based_recognizers
import graph_recognition.recognizers_n_2
import graph_recognition.recognizers_n_3
import graph_recognition.recognizers_n_4
import graph_recognition.recognizers_n_6
import networkx
import networkx.algorithms.asteroidal
import os
import sys
import unittest


class Test_gc_151_and_ancestors(unittest.TestCase):
    """A generic test case for class gc_151 and all its ancestors that have not
    already been covered by other tests."""
    @classmethod
    def setUpClass(self):
        """Stores positive and negative instances to test."""
        super(Test_gc_151_and_ancestors, self).setUpClass()
        self.positive = list()
        basedir = "tests/test_data/cographs=gc_151"
        print(self.__class__.__qualname__.join("[]"), "initialising positive instances from tests/test_data/cographs=gc_151...", end=" ")
        sys.stdout.flush()
        for dataset in sorted(os.listdir(basedir), key=lambda x: os.stat(os.path.join(basedir, x)).st_size)[:5]:
            if dataset.endswith(".g6"):
                data = networkx.read_graph6(os.path.join(basedir, dataset))
            elif dataset.endswith(".s6"):
                data = networkx.read_sparse6(os.path.join(basedir, dataset))
            else:
                continue
            if not isinstance(data, list):
                data = [data]
            self.positive.extend(data)
        print("done.")

    def test_gc_151(self):
        """Tests positive instances for class gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_152(g) for g in self.positive))
        print("done.")
    def test_gc_188(self):
        """Tests positive instances for class gc_188. gc_188 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_188(g) for g in self.positive))
        print("done.")
    def test_gc_510(self):
        """Tests positive instances for class gc_510. gc_510 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_510(g) for g in self.positive))
        print("done.")
    def test_gc_475(self):
        """Tests positive instances for class gc_475. gc_475 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_475(g) for g in self.positive))
        print("done.")
    def test_AUTO_1508(self):
        """Tests positive instances for class AUTO_1508. AUTO_1508 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1508(g) for g in self.positive))
        print("done.")
    def test_AUTO_1503(self):
        """Tests positive instances for class AUTO_1503. AUTO_1503 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1503(g) for g in self.positive))
        print("done.")
    def test_gc_177(self):
        """Tests positive instances for class gc_177. gc_177 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhdg_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_2142(self):
        """Tests positive instances for class AUTO_2142. AUTO_2142 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2142(g) for g in self.positive))
        print("done.")
    def test_AUTO_1538(self):
        """Tests positive instances for class AUTO_1538. AUTO_1538 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_p5_co_p_anti_hole_free(g) for g in self.positive))
        print("done.")
    def test_gc_1032(self):
        """Tests positive instances for class gc_1032. gc_1032 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_co_fork_hole_free(g) for g in self.positive))
        print("done.")
    def test_gc_811(self):
        """Tests positive instances for class gc_811. gc_811 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_811(g) for g in self.positive))
        print("done.")
    def test_gc_376(self):
        """Tests positive instances for class gc_376. gc_376 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_376(g) for g in self.positive))
        print("done.")
    def test_AUTO_2138(self):
        """Tests positive instances for class AUTO_2138. AUTO_2138 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2138(g) for g in self.positive))
        print("done.")
    def test_AUTO_1470(self):
        """Tests positive instances for class AUTO_1470. AUTO_1470 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1470(g) for g in self.positive))
        print("done.")
    def test_gc_619(self):
        """Tests positive instances for class gc_619. gc_619 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_619(g) for g in self.positive))
        print("done.")
    def test_gc_930(self):
        """Tests positive instances for class gc_930. gc_930 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_930(g) for g in self.positive))
        print("done.")
    def test_gc_520(self):
        """Tests positive instances for class gc_520. gc_520 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_520(g) for g in self.positive))
        print("done.")
    def test_gc_592(self):
        """Tests positive instances for class gc_592. gc_592 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_592(g) for g in self.positive))
        print("done.")
    def test_gc_435(self):
        """Tests positive instances for class gc_435. gc_435 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_weakly_chordal(g) for g in self.positive))
        print("done.")
    def test_gc_324(self):
        """Tests positive instances for class gc_324. gc_324 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_324(g) for g in self.positive))
        print("done.")
    def test_AUTO_1505(self):
        """Tests positive instances for class AUTO_1505. AUTO_1505 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1505(g) for g in self.positive))
        print("done.")
    def test_AUTO_1507(self):
        """Tests positive instances for class AUTO_1507. AUTO_1507 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1507(g) for g in self.positive))
        print("done.")
    def test_gc_489(self):
        """Tests positive instances for class gc_489. gc_489 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_24(g) for g in self.positive))
        print("done.")
    def test_gc_201(self):
        """Tests positive instances for class gc_201. gc_201 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_3.is_pseudo_modular(g) for g in self.positive))
        print("done.")
    def test_AUTO_2097(self):
        """Tests positive instances for class AUTO_2097. AUTO_2097 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2097(g) for g in self.positive))
        print("done.")
    def test_gc_477(self):
        """Tests positive instances for class gc_477. gc_477 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_477(g) for g in self.positive))
        print("done.")
    def test_gc_814(self):
        """Tests positive instances for class gc_814. gc_814 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_814(g) for g in self.positive))
        print("done.")
    def test_gc_845(self):
        """Tests positive instances for class gc_845. gc_845 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_845(g) for g in self.positive))
        print("done.")
    def test_gc_590(self):
        """Tests positive instances for class gc_590. gc_590 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_590(g) for g in self.positive))
        print("done.")
    def test_AUTO_2096(self):
        """Tests positive instances for class AUTO_2096. AUTO_2096 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2096(g) for g in self.positive))
        print("done.")
    def test_gc_398(self):
        """Tests positive instances for class gc_398. gc_398 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_398(g) for g in self.positive))
        print("done.")
    def test_AUTO_2134(self):
        """Tests positive instances for class AUTO_2134. AUTO_2134 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2134(g) for g in self.positive))
        print("done.")
    def test_gc_759(self):
        """Tests positive instances for class gc_759. gc_759 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_759(g) for g in self.positive))
        print("done.")
    def test_gc_17(self):
        """Tests positive instances for class gc_17. gc_17 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_17(g) for g in self.positive))
        print("done.")
    def test_gc_351(self):
        """Tests positive instances for class gc_351. gc_351 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhda_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_1476(self):
        """Tests positive instances for class AUTO_1476. AUTO_1476 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1476(g) for g in self.positive))
        print("done.")
    def test_gc_402(self):
        """Tests positive instances for class gc_402. gc_402 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_402(g) for g in self.positive))
        print("done.")
    def test_AUTO_1509(self):
        """Tests positive instances for class AUTO_1509. AUTO_1509 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1509(g) for g in self.positive))
        print("done.")
    def test_AUTO_1488(self):
        """Tests positive instances for class AUTO_1488. AUTO_1488 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1488(g) for g in self.positive))
        print("done.")
    def test_AUTO_1528(self):
        """Tests positive instances for class AUTO_1528. AUTO_1528 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1528(g) for g in self.positive))
        print("done.")
    def test_gc_818(self):
        """Tests positive instances for class gc_818. gc_818 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_818(g) for g in self.positive))
        print("done.")
    def test_AUTO_407(self):
        """Tests positive instances for class AUTO_407. AUTO_407 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_407(g) for g in self.positive))
        print("done.")
    def test_AUTO_92(self):
        """Tests positive instances for class AUTO_92. AUTO_92 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_92(g) for g in self.positive))
        print("done.")
    def test_gc_1276(self):
        """Tests positive instances for class gc_1276. gc_1276 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1276(g) for g in self.positive))
        print("done.")
    def test_AUTO_1752(self):
        """Tests positive instances for class AUTO_1752. AUTO_1752 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_P5_S3_anti_hole_co_domino_co_gem(g) for g in self.positive))
        print("done.")
    def test_AUTO_1531(self):
        """Tests positive instances for class AUTO_1531. AUTO_1531 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_p5_co_a_co_domino_antihole_free(g) for g in self.positive))
        print("done.")
    def test_gc_490(self):
        """Tests positive instances for class gc_490. gc_490 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_359(g) for g in self.positive))
        print("done.")
    def test_AUTO_1644(self):
        """Tests positive instances for class AUTO_1644. AUTO_1644 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_anti_hole_fork_free(g) for g in self.positive))
        print("done.")
    def test_gc_840(self):
        """Tests positive instances for class gc_840. gc_840 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_840(g) for g in self.positive))
        print("done.")
    def test_AUTO_1496(self):
        """Tests positive instances for class AUTO_1496. AUTO_1496 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1496(g) for g in self.positive))
        print("done.")
    def test_gc_678(self):
        """Tests positive instances for class gc_678. gc_678 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_678(g) for g in self.positive))
        print("done.")
    def test_AUTO_1497(self):
        """Tests positive instances for class AUTO_1497. AUTO_1497 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1497(g) for g in self.positive))
        print("done.")
    def test_gc_463(self):
        """Tests positive instances for class gc_463. gc_463 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_463(g) for g in self.positive))
        print("done.")
    def test_AUTO_1456(self):
        """Tests positive instances for class AUTO_1456. AUTO_1456 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1456(g) for g in self.positive))
        print("done.")
    def test_gc_428(self):
        """Tests positive instances for class gc_428. gc_428 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_428(g) for g in self.positive))
        print("done.")
    def test_gc_679(self):
        """Tests positive instances for class gc_679. gc_679 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_p5_anti_hole_free(g) for g in self.positive))
        print("done.")
    def test_gc_813(self):
        """Tests positive instances for class gc_813. gc_813 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_813(g) for g in self.positive))
        print("done.")
    def test_AUTO_497(self):
        """Tests positive instances for class AUTO_497. AUTO_497 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_497(g) for g in self.positive))
        print("done.")
    def test_gc_519(self):
        """Tests positive instances for class gc_519. gc_519 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_519(g) for g in self.positive))
        print("done.")
    def test_gc_839(self):
        """Tests positive instances for class gc_839. gc_839 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_839(g) for g in self.positive))
        print("done.")
    def test_gc_479(self):
        """Tests positive instances for class gc_479. gc_479 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_479(g) for g in self.positive))
        print("done.")
    def test_gc_308(self):
        """Tests positive instances for class gc_308. gc_308 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_308(g) for g in self.positive))
        print("done.")
    def test_gc_1357(self):
        """Tests positive instances for class gc_1357. gc_1357 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1357(g) for g in self.positive))
        print("done.")
    def test_gc_516(self):
        """Tests positive instances for class gc_516. gc_516 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_516(g) for g in self.positive))
        print("done.")
    def test_gc_538(self):
        """Tests positive instances for class gc_538. gc_538 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_538(g) for g in self.positive))
        print("done.")
    def test_gc_798(self):
        """Tests positive instances for class gc_798. gc_798 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_798(g) for g in self.positive))
        print("done.")
    def test_gc_1330(self):
        """Tests positive instances for class gc_1330. gc_1330 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1330(g) for g in self.positive))
        print("done.")
    def test_gc_767(self):
        """Tests positive instances for class gc_767. gc_767 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_2.is_comparability(g) for g in self.positive))
        print("done.")
    def test_gc_758(self):
        """Tests positive instances for class gc_758. gc_758 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_758(g) for g in self.positive))
        print("done.")
    def test_AUTO_2090(self):
        """Tests positive instances for class AUTO_2090. AUTO_2090 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2090(g) for g in self.positive))
        print("done.")
    def test_AUTO_1533(self):
        """Tests positive instances for class AUTO_1533. AUTO_1533 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1533(g) for g in self.positive))
        print("done.")
    def test_AUTO_1075(self):
        """Tests positive instances for class AUTO_1075. AUTO_1075 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_P5_anti_hole_co_domino_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_2127(self):
        """Tests positive instances for class AUTO_2127. AUTO_2127 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_3.is_co_at_free(g) for g in self.positive))
        print("done.")
    def test_gc_779(self):
        """Tests positive instances for class gc_779. gc_779 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_779(g) for g in self.positive))
        print("done.")
    def test_gc_513(self):
        """Tests positive instances for class gc_513. gc_513 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_513(g) for g in self.positive))
        print("done.")
    def test_gc_563(self):
        """Tests positive instances for class gc_563. gc_563 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_563(g) for g in self.positive))
        print("done.")
    def test_gc_61(self):
        """Tests positive instances for class gc_61. gc_61 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(networkx.algorithms.asteroidal.is_at_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_1473(self):
        """Tests positive instances for class AUTO_1473. AUTO_1473 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1473(g) for g in self.positive))
        print("done.")
    def test_AUTO_1514(self):
        """Tests positive instances for class AUTO_1514. AUTO_1514 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1514(g) for g in self.positive))
        print("done.")
    def test_gc_438(self):
        """Tests positive instances for class gc_438. gc_438 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_438(g) for g in self.positive))
        print("done.")
    def test_gc_113(self):
        """Tests positive instances for class gc_113. gc_113 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhp_free(g) for g in self.positive))
        print("done.")
    def test_gc_373(self):
        """Tests positive instances for class gc_373. gc_373 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_373(g) for g in self.positive))
        print("done.")
    def test_gc_386(self):
        """Tests positive instances for class gc_386. gc_386 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_386(g) for g in self.positive))
        print("done.")
    def test_AUTO_2141(self):
        """Tests positive instances for class AUTO_2141. AUTO_2141 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2141(g) for g in self.positive))
        print("done.")
    def test_gc_189(self):
        """Tests positive instances for class gc_189. gc_189 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_189(g) for g in self.positive))
        print("done.")
    def test_AUTO_1495(self):
        """Tests positive instances for class AUTO_1495. AUTO_1495 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1495(g) for g in self.positive))
        print("done.")
    def test_gc_466(self):
        """Tests positive instances for class gc_466. gc_466 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_466(g) for g in self.positive))
        print("done.")
    def test_gc_816(self):
        """Tests positive instances for class gc_816. gc_816 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_816(g) for g in self.positive))
        print("done.")
    def test_gc_647(self):
        """Tests positive instances for class gc_647. gc_647 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_647(g) for g in self.positive))
        print("done.")
    def test_gc_574(self):
        """Tests positive instances for class gc_574. gc_574 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_574(g) for g in self.positive))
        print("done.")
    def test_gc_648(self):
        """Tests positive instances for class gc_648. gc_648 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_648(g) for g in self.positive))
        print("done.")
    def test_gc_397(self):
        """Tests positive instances for class gc_397. gc_397 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_397(g) for g in self.positive))
        print("done.")
    def test_gc_474(self):
        """Tests positive instances for class gc_474. gc_474 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_474(g) for g in self.positive))
        print("done.")
    def test_gc_354(self):
        """Tests positive instances for class gc_354. gc_354 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_354(g) for g in self.positive))
        print("done.")
    def test_gc_534(self):
        """Tests positive instances for class gc_534. gc_534 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hereditary_welsh_powell_perfect(g) for g in self.positive))
        print("done.")
    def test_gc_273(self):
        """Tests positive instances for class gc_273. gc_273 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_273(g) for g in self.positive))
        print("done.")
    def test_gc_437(self):
        """Tests positive instances for class gc_437. gc_437 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hole_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_1464(self):
        """Tests positive instances for class AUTO_1464. AUTO_1464 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1464(g) for g in self.positive))
        print("done.")
    def test_gc_436(self):
        """Tests positive instances for class gc_436. gc_436 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_436(g) for g in self.positive))
        print("done.")
    def test_gc_410(self):
        """Tests positive instances for class gc_410. gc_410 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_410(g) for g in self.positive))
        print("done.")
    def test_AUTO_1459(self):
        """Tests positive instances for class AUTO_1459. AUTO_1459 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1459(g) for g in self.positive))
        print("done.")
    def test_gc_476(self):
        """Tests positive instances for class gc_476. gc_476 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_476(g) for g in self.positive))
        print("done.")
    def test_gc_669(self):
        """Tests positive instances for class gc_669. gc_669 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_669(g) for g in self.positive))
        print("done.")
    def test_gc_423(self):
        """Tests positive instances for class gc_423. gc_423 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_423(g) for g in self.positive))
        print("done.")
    def test_AUTO_1490(self):
        """Tests positive instances for class AUTO_1490. AUTO_1490 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1490(g) for g in self.positive))
        print("done.")
    def test_gc_757(self):
        """Tests positive instances for class gc_757. gc_757 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_757(g) for g in self.positive))
        print("done.")
    def test_gc_396(self):
        """Tests positive instances for class gc_396. gc_396 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_396(g) for g in self.positive))
        print("done.")
    def test_gc_737(self):
        """Tests positive instances for class gc_737. gc_737 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_737(g) for g in self.positive))
        print("done.")
    def test_gc_1230(self):
        """Tests positive instances for class gc_1230. gc_1230 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_domino_gem_hole_house_net_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_1892(self):
        """Tests positive instances for class AUTO_1892. AUTO_1892 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1892(g) for g in self.positive))
        print("done.")
    def test_AUTO_1510(self):
        """Tests positive instances for class AUTO_1510. AUTO_1510 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1510(g) for g in self.positive))
        print("done.")
    def test_gc_433(self):
        """Tests positive instances for class gc_433. gc_433 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_433(g) for g in self.positive))
        print("done.")
    def test_AUTO_2104(self):
        """Tests positive instances for class AUTO_2104. AUTO_2104 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2104(g) for g in self.positive))
        print("done.")
    def test_gc_588(self):
        """Tests positive instances for class gc_588. gc_588 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_588(g) for g in self.positive))
        print("done.")
    def test_AUTO_2088(self):
        """Tests positive instances for class AUTO_2088. AUTO_2088 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_co_hereditary_welsh_powell_perfect(g) for g in self.positive))
        print("done.")
    def test_AUTO_2125(self):
        """Tests positive instances for class AUTO_2125. AUTO_2125 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2125(g) for g in self.positive))
        print("done.")
    def test_AUTO_1520(self):
        """Tests positive instances for class AUTO_1520. AUTO_1520 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1520(g) for g in self.positive))
        print("done.")
    def test_AUTO_1820(self):
        """Tests positive instances for class AUTO_1820. AUTO_1820 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1820(g) for g in self.positive))
        print("done.")
    def test_AUTO_1534(self):
        """Tests positive instances for class AUTO_1534. AUTO_1534 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_p5_anti_hole_co_gem_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_1461(self):
        """Tests positive instances for class AUTO_1461. AUTO_1461 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1461(g) for g in self.positive))
        print("done.")
    def test_AUTO_202(self):
        """Tests positive instances for class AUTO_202. AUTO_202 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_202(g) for g in self.positive))
        print("done.")
    def test_gc_819(self):
        """Tests positive instances for class gc_819. gc_819 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_819(g) for g in self.positive))
        print("done.")
    def test_gc_224(self):
        """Tests positive instances for class gc_224. gc_224 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_224(g) for g in self.positive))
        print("done.")
    def test_gc_842(self):
        """Tests positive instances for class gc_842. gc_842 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_842(g) for g in self.positive))
        print("done.")
    def test_gc_537(self):
        """Tests positive instances for class gc_537. gc_537 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_537(g) for g in self.positive))
        print("done.")
    def test_gc_260(self):
        """Tests positive instances for class gc_260. gc_260 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_260(g) for g in self.positive))
        print("done.")
    def test_gc_8(self):
        """Tests positive instances for class gc_8. gc_8 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_6.is_p4_tidy(g) for g in self.positive))
        print("done.")
    def test_AUTO_94(self):
        """Tests positive instances for class AUTO_94. AUTO_94 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_anti_hole_free(g) for g in self.positive))
        print("done.")
    def test_gc_572(self):
        """Tests positive instances for class gc_572. gc_572 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_572(g) for g in self.positive))
        print("done.")
    def test_AUTO_2124(self):
        """Tests positive instances for class AUTO_2124. AUTO_2124 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2124(g) for g in self.positive))
        print("done.")
    def test_gc_518(self):
        """Tests positive instances for class gc_518. gc_518 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_518(g) for g in self.positive))
        print("done.")
    def test_AUTO_3(self):
        """Tests positive instances for class AUTO_3. AUTO_3 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_3(g) for g in self.positive))
        print("done.")
    def test_gc_511(self):
        """Tests positive instances for class gc_511. gc_511 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_511(g) for g in self.positive))
        print("done.")
    def test_AUTO_41(self):
        """Tests positive instances for class AUTO_41. AUTO_41 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_41(g) for g in self.positive))
        print("done.")
    def test_gc_439(self):
        """Tests positive instances for class gc_439. gc_439 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_439(g) for g in self.positive))
        print("done.")
    def test_AUTO_1504(self):
        """Tests positive instances for class AUTO_1504. AUTO_1504 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1504(g) for g in self.positive))
        print("done.")
    def test_gc_480(self):
        """Tests positive instances for class gc_480. gc_480 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_480(g) for g in self.positive))
        print("done.")
    def test_AUTO_1498(self):
        """Tests positive instances for class AUTO_1498. AUTO_1498 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1498(g) for g in self.positive))
        print("done.")
    def test_gc_517(self):
        """Tests positive instances for class gc_517. gc_517 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_517(g) for g in self.positive))
        print("done.")
    def test_AUTO_1465(self):
        """Tests positive instances for class AUTO_1465. AUTO_1465 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1465(g) for g in self.positive))
        print("done.")
    def test_gc_638(self):
        """Tests positive instances for class gc_638. gc_638 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_638(g) for g in self.positive))
        print("done.")
    def test_gc_585(self):
        """Tests positive instances for class gc_585. gc_585 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_585(g) for g in self.positive))
        print("done.")
    def test_gc_683(self):
        """Tests positive instances for class gc_683. gc_683 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_683(g) for g in self.positive))
        print("done.")
    def test_gc_756(self):
        """Tests positive instances for class gc_756. gc_756 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_756(g) for g in self.positive))
        print("done.")
    def test_AUTO_1484(self):
        """Tests positive instances for class AUTO_1484. AUTO_1484 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1484(g) for g in self.positive))
        print("done.")
    def test_AUTO_734(self):
        """Tests positive instances for class AUTO_734. AUTO_734 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_734(g) for g in self.positive))
        print("done.")
    def test_gc_268(self):
        """Tests positive instances for class gc_268. gc_268 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_268(g) for g in self.positive))
        print("done.")
    def test_AUTO_329(self):
        """Tests positive instances for class AUTO_329. AUTO_329 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_329(g) for g in self.positive))
        print("done.")
    def test_AUTO_1502(self):
        """Tests positive instances for class AUTO_1502. AUTO_1502 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1502(g) for g in self.positive))
        print("done.")
    def test_gc_391(self):
        """Tests positive instances for class gc_391. gc_391 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_391(g) for g in self.positive))
        print("done.")
    def test_gc_216(self):
        """Tests positive instances for class gc_216. gc_216 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhg_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_2(self):
        """Tests positive instances for class AUTO_2. AUTO_2 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2(g) for g in self.positive))
        print("done.")
    def test_gc_512(self):
        """Tests positive instances for class gc_512. gc_512 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_512(g) for g in self.positive))
        print("done.")
    def test_gc_228(self):
        """Tests positive instances for class gc_228. gc_228 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hh_free(g) for g in self.positive))
        print("done.")
    def test_gc_372(self):
        """Tests positive instances for class gc_372. gc_372 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_372(g) for g in self.positive))
        print("done.")
    def test_AUTO_1535(self):
        """Tests positive instances for class AUTO_1535. AUTO_1535 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_P5_anti_hole_co_domino_co_gem_free(g) for g in self.positive))
        print("done.")
    def test_gc_478(self):
        """Tests positive instances for class gc_478. gc_478 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_478(g) for g in self.positive))
        print("done.")
    def test_AUTO_2110(self):
        """Tests positive instances for class AUTO_2110. AUTO_2110 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2110(g) for g in self.positive))
        print("done.")
    def test_gc_230(self):
        """Tests positive instances for class gc_230. gc_230 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_hhd_free(g) for g in self.positive))
        print("done.")
    def test_AUTO_2089(self):
        """Tests positive instances for class AUTO_2089. AUTO_2089 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2089(g) for g in self.positive))
        print("done.")
    def test_gc_1108(self):
        """Tests positive instances for class gc_1108. gc_1108 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1108(g) for g in self.positive))
        print("done.")
    def test_gc_361(self):
        """Tests positive instances for class gc_361. gc_361 is an ancestor of gc_151."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_house_free(g) for g in self.positive))
        print("done.")