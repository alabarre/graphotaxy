"""Anthony Labarre © 2024

This file was automatically generated by generate_tests.py on 2024-07-13. It
consists of a test suite for recognizers that identify gc_1316 and / or all its
ancestors. Depending on which recognizers have been implemented, some class ids
may have been substituted with equivalent ids according to ISGCI.

Some of the ancestors may have been purposefully omitted from this file, either
in order to avoid duplicating tests or because the needed recognizers were
missing.

Check https://www.graphclasses.org/classes/gc_1316 for more information.

"""
# Imports ---------------------------------------------------------------------
import graph_recognition.fisc_based_recognizers
import graph_recognition.profitable_hereditary_n
import graph_recognition.recognizers_n
import graph_recognition.recognizers_n_2
import graph_recognition.recognizers_n_4
import graph_recognition.recognizers_n_5
import networkx
import networkx.algorithms.components.biconnected
import os
import sys
import unittest


class Test_gc_1316_and_ancestors(unittest.TestCase):
    """A generic test case for class gc_1316 and all its ancestors that have not
    already been covered by other tests."""
    @classmethod
    def setUpClass(self):
        """Stores positive and negative instances to test."""
        super(Test_gc_1316_and_ancestors, self).setUpClass()
        self.positive = list()
        basedir = "tests/test_data/cubic-and-hamiltonian=gc_1316"
        print(self.__class__.__qualname__.join("[]"), "initialising positive instances from tests/test_data/cubic-and-hamiltonian=gc_1316...", end=" ")
        sys.stdout.flush()
        for dataset in sorted(os.listdir(basedir), key=lambda x: os.stat(os.path.join(basedir, x)).st_size)[:5]:
            if dataset.endswith(".g6"):
                data = networkx.read_graph6(os.path.join(basedir, dataset))
            elif dataset.endswith(".s6"):
                data = networkx.read_sparse6(os.path.join(basedir, dataset))
            else:
                continue
            if not isinstance(data, list):
                data = [data]
            self.positive.extend(data)
        print("done.")

    def test_AUTO_329(self):
        """Tests positive instances for class AUTO_329. AUTO_329 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_329(g) for g in self.positive))
        print("done.")
    def test_gc_1343(self):
        """Tests positive instances for class gc_1343. gc_1343 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1343(g) for g in self.positive))
        print("done.")
    def test_AUTO_1820(self):
        """Tests positive instances for class AUTO_1820. AUTO_1820 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_1820(g) for g in self.positive))
        print("done.")
    def test_gc_700(self):
        """Tests positive instances for class gc_700. gc_700 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_700(g) for g in self.positive))
        print("done.")
    def test_gc_388(self):
        """Tests positive instances for class gc_388. gc_388 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_388(g) for g in self.positive))
        print("done.")
    def test_gc_720(self):
        """Tests positive instances for class gc_720. gc_720 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.profitable_hereditary_n.is_maximum_degree_3(g) for g in self.positive))
        print("done.")
    def test_gc_1254(self):
        """Tests positive instances for class gc_1254. gc_1254 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_2.is_locally_chordal(g) for g in self.positive))
        print("done.")
    def test_AUTO_2097(self):
        """Tests positive instances for class AUTO_2097. AUTO_2097 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2097(g) for g in self.positive))
        print("done.")
    def test_gc_871(self):
        """Tests positive instances for class gc_871. gc_871 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_871(g) for g in self.positive))
        print("done.")
    def test_gc_1362(self):
        """Tests positive instances for class gc_1362. gc_1362 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_2_edge_connected(g) for g in self.positive))
        print("done.")
    def test_AUTO_41(self):
        """Tests positive instances for class AUTO_41. AUTO_41 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_41(g) for g in self.positive))
        print("done.")
    def test_gc_1090(self):
        """Tests positive instances for class gc_1090. gc_1090 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.profitable_hereditary_n.is_maximum_degree_7(g) for g in self.positive))
        print("done.")
    def test_gc_1139(self):
        """Tests positive instances for class gc_1139. gc_1139 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.profitable_hereditary_n.is_maximum_degree_5(g) for g in self.positive))
        print("done.")
    def test_AUTO_202(self):
        """Tests positive instances for class AUTO_202. AUTO_202 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_202(g) for g in self.positive))
        print("done.")
    def test_gc_1344(self):
        """Tests positive instances for class gc_1344. gc_1344 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_1344(g) for g in self.positive))
        print("done.")
    def test_AUTO_224(self):
        """Tests positive instances for class AUTO_224. AUTO_224 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_224(g) for g in self.positive))
        print("done.")
    def test_gc_771(self):
        """Tests positive instances for class gc_771. gc_771 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(networkx.algorithms.components.biconnected.is_biconnected(g) for g in self.positive))
        print("done.")
    def test_gc_1119(self):
        """Tests positive instances for class gc_1119. gc_1119 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.profitable_hereditary_n.is_maximum_degree_6(g) for g in self.positive))
        print("done.")
    def test_AUTO_2110(self):
        """Tests positive instances for class AUTO_2110. AUTO_2110 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2110(g) for g in self.positive))
        print("done.")
    def test_gc_609(self):
        """Tests positive instances for class gc_609. gc_609 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_4.is_pretty(g) for g in self.positive))
        print("done.")
    def test_AUTO_497(self):
        """Tests positive instances for class AUTO_497. AUTO_497 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_497(g) for g in self.positive))
        print("done.")
    def test_AUTO_2096(self):
        """Tests positive instances for class AUTO_2096. AUTO_2096 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2096(g) for g in self.positive))
        print("done.")
    def test_gc_715(self):
        """Tests positive instances for class gc_715. gc_715 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_713(g) for g in self.positive))
        print("done.")
    def test_AUTO_2123(self):
        """Tests positive instances for class AUTO_2123. AUTO_2123 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_AUTO_2123(g) for g in self.positive))
        print("done.")
    def test_gc_717(self):
        """Tests positive instances for class gc_717. gc_717 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.profitable_hereditary_n.is_maximum_degree_4(g) for g in self.positive))
        print("done.")
    def test_gc_625(self):
        """Tests positive instances for class gc_625. gc_625 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_625(g) for g in self.positive))
        print("done.")
    def test_gc_1100(self):
        """Tests positive instances for class gc_1100. gc_1100 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n.is_cubic(g) for g in self.positive))
        print("done.")
    def test_gc_54(self):
        """Tests positive instances for class gc_54. gc_54 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.recognizers_n_5.is_clique_helly(g) for g in self.positive))
        print("done.")
    def test_gc_376(self):
        """Tests positive instances for class gc_376. gc_376 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_376(g) for g in self.positive))
        print("done.")
    def test_gc_748(self):
        """Tests positive instances for class gc_748. gc_748 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_748(g) for g in self.positive))
        print("done.")
    def test_gc_354(self):
        """Tests positive instances for class gc_354. gc_354 is an ancestor of gc_1316."""
        print(
            self._testMethodName.join("[]"), "testing", len(self.positive), "graphs", end=" "
        )
        sys.stdout.flush()
        self.assertTrue(all(graph_recognition.fisc_based_recognizers.is_gc_354(g) for g in self.positive))
        print("done.")