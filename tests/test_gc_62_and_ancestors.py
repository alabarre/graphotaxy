"""Anthony Labarre © 2025

This file was automatically generated by generate_tests.py on 2025-12-19. It consists of a test
suite for recognizers that identify gc_62 and a number of related classes. Specifically, **if** the
required recognizers are available, the test suite should contain:

    - a positive test for gc_62;
    - a positive test for each recognizable ancestor of gc_62;
    - a negative test for each class excluded by gc_62;
    - and a negative test for each descendant of each class excluded by gc_62.

Depending on which recognizers have been implemented and which representative of set of equivalent
class has been chosen, some class ids may have been substituted with equivalent ids according to
ISGCI.

Some of the ancestors may have been purposefully omitted from this file, either in order to avoid
duplicating tests or because the needed recognizers were missing.

Check https://www.graphclasses.org/classes/gc_62 for more information.
"""

# Imports -----------------------------------------------------------------------------------------
import os
import sys
import unittest
import networkx


class Test_gc_62_and_ancestors(unittest.TestCase):
    """A generic test case for class gc_62 and all its ancestors that have not already been covered
    by other tests."""

    @classmethod
    def setUpClass(self) -> None:
        """Stores positive and negative instances to test."""
        super(Test_gc_62_and_ancestors, self).setUpClass()
        self.positive = []
        basedir = "./tests/test_data/claw-free=gc_62"
        print(
            self.__qualname__.join("[]"),
            "initializing positive instances from",
            basedir,
            "...",
            end=" ",
        )
        sys.stdout.flush()
        for dataset in sorted(
            os.listdir(basedir), key=lambda x: os.stat(os.path.join(basedir, x)).st_size
        )[:5]:
            if dataset.endswith(".g6"):
                data = networkx.read_graph6(os.path.join(basedir, dataset))
            elif dataset.endswith(".s6"):
                data = networkx.read_sparse6(os.path.join(basedir, dataset))
            else:
                continue
            if not isinstance(data, list):
                data = [data]
            self.positive.extend(data)
        print("done.")


# No recognizer was found for class gc_62 or any equivalent class, so no
# test could be generated for that specific class
# Generated tests for ancestors of base class gc_62:
