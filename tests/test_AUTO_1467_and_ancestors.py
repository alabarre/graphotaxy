"""Anthony Labarre © 2026

This file was automatically generated by generate_tests.py on 2026-02-22. It consists of a test
suite for recognizers that identify AUTO_1467 and a number of related classes. Specifically, **if**
the required recognizers are available, the test suite should contain:

    - a positive test for AUTO_1467;
    - a positive test for each recognizable ancestor of AUTO_1467;
    - a negative test for each class excluded by AUTO_1467;
    - and a negative test for each descendant of each class excluded by AUTO_1467.

Depending on which recognizers have been implemented and which representative of set of equivalent
class has been chosen, some class ids may have been substituted with equivalent ids according to
ISGCI.

Some of the ancestors may have been purposefully omitted from this file, either in order to avoid
duplicating tests or because the needed recognizers were missing.

Check https://www.graphclasses.org/classes/AUTO_1467 for more information.
"""

# Imports -----------------------------------------------------------------------------------------
import os
import sys
import unittest
import networkx


class Test_AUTO_1467_and_ancestors(unittest.TestCase):
    """A generic test case for class AUTO_1467 and all its ancestors that have not already been
    covered by other tests."""

    @classmethod
    def setUpClass(self) -> None:
        """Stores positive and negative instances to test."""
        super(Test_AUTO_1467_and_ancestors, self).setUpClass()
        self.positive = []
        basedir = "./tests/test_data/(co-claw,co-diamond)-free=AUTO_1467"
        print(
            self.__qualname__.join("[]"),
            "initializing positive instances from",
            basedir,
            "...",
            end=" ",
        )
        sys.stdout.flush()

        # the slice below restricts tests to 5 files in order to keep the running times reasonable
        for dataset in sorted(
            os.listdir(basedir), key=lambda x: os.stat(os.path.join(basedir, x)).st_size
        )[:5]:
            if dataset.endswith(".g6"):
                data = networkx.read_graph6(os.path.join(basedir, dataset))
            elif dataset.endswith(".s6"):
                data = networkx.read_sparse6(os.path.join(basedir, dataset))
            else:
                continue
            if not isinstance(data, list):
                data = [data]
            self.positive.extend(data)
        print("done.")

    # No recognizer was found for class AUTO_1467 or any equivalent class, so no


# test could be generated for that specific class
# Generated tests for ancestors of base class AUTO_1467:
